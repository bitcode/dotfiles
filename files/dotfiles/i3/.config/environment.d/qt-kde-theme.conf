# Without this, Qt/KDE apps (Dolphin, etc.) launched outside a full Plasma
# session ignore kdeglobals/color-schemes and fall back to a default palette
# that can clash with the icon theme (e.g. unreadable icon-view labels).
# Requires the plasma-integration package.
QT_QPA_PLATFORMTHEME=kde
