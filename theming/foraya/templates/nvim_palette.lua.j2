-- Foraya palette data (contrast={{ meta.contrast }}, accent={{ meta.accent }})
-- Generated by theming/foraya/generate.py — do not hand-edit, edit
-- theming/foraya/palette.yml and regenerate instead.
-- Consumed by colors/foraya.lua; reusable by lualine/statusline plugins later.

return {
  meta = { name = "{{ meta.name }}", contrast = "{{ meta.contrast }}", accent = "{{ meta.accent }}" },
  bg = {
    base = "{{ bg.base }}",
    lift = "{{ bg.lift }}",
    panel = "{{ bg.panel }}",
    popup = "{{ bg.popup }}",
    line = "{{ bg.line }}",
    gutter = "{{ bg.gutter }}",
  },
  fg = {
    primary = "{{ fg.primary }}",
    dim = "{{ fg.dim }}",
  },
  neutral = {
    dim = "{{ neutral.dim }}",
    mid = "{{ neutral.mid }}",
    bright = "{{ neutral.bright }}",
  },
  cursor = {
    color = "{{ cursor.color }}",
    text = "{{ cursor.text }}",
  },
  selection = {
    background = "{{ selection.background }}",
    foreground = "{{ selection.foreground }}",
  },
  accents = {
    red = "{{ accents.red }}",
    orange = "{{ accents.orange }}",
    yellow = "{{ accents.yellow }}",
    green = "{{ accents.green }}",
    aqua = "{{ accents.aqua }}",
    cyan = "{{ accents.cyan }}",
    blue = "{{ accents.blue }}",
    purple = "{{ accents.purple }}",
  },
  special = {
    gold = "{{ special.gold }}",
    hint = "{{ special.hint }}",
    info = "{{ special.info }}",
    vcs = {
      added = "{{ special.vcs.added }}",
      modified = "{{ special.vcs.modified }}",
      removed = "{{ special.vcs.removed }}",
    },
  },
}
